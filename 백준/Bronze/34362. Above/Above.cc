#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    double N;
    cin >> N;
    cout << fixed << setprecision(4) << N - 0.3 << "\n";
    return 0;
}