#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long M, K, C;
    cin >> M >> K >> C;
    cout << M * C << "\n";
    return 0;
}